import { createStore } from "vuex";

export default createStore({
  state: {
    user: {
      data: { name: "Frank" },
      token: null,
    },
  },
  getters: {},
  mutations: {},
  actions: {},
  modules: {},
});
